{
  "name": "dockview",
  "version": "0.1.0",
  "private": true,
  "description": "A lightweight Docker server dashboard",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
